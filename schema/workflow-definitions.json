{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/hiivmind/hiivmind-blueprint-lib/main/schema/workflow-definitions.json",
  "$comment": "Schema version 2.0 - Defines the definitions block for workflow.yaml files",
  "title": "Workflow Definitions Schema",
  "description": "Schema for the optional 'definitions' block in workflow.yaml files that specifies where type definitions are loaded from",
  "type": "object",
  "properties": {
    "source": {
      "$ref": "common.json#/$defs/repo_reference",
      "description": "Type definitions source. Use shorthand format 'owner/repo@version' which resolves to raw GitHub URLs."
    },
    "extensions": {
      "type": "array",
      "description": "Additional type definition packages to load",
      "items": {
        "$ref": "common.json#/$defs/repo_reference"
      }
    }
  },
  "required": ["source"],
  "additionalProperties": false,
  "examples": [
    {
      "$comment": "Simple: Shorthand reference",
      "source": "hiivmind/hiivmind-blueprint-lib@v2.0.0"
    },
    {
      "$comment": "With extensions",
      "source": "hiivmind/hiivmind-blueprint-lib@v2.0.0",
      "extensions": [
        "mycorp/custom-types@v1.0.0"
      ]
    }
  ]
}
